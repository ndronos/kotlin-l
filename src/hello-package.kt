package hello                   // optional package header
                                // package-level function, which takes
fun main(args: Array<String>) { // an Array of strings as a parameter
    println("Hello World!")     // Semicolons are optional
}